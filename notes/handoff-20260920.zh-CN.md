# 给学长：MergeNet 还需要完成的事项（2026-09-20）

> 2026-09-22 更新：详细交付格式、论文落位和 P01--P12 空槽见
> `notes/collaborator-request-20260922.zh-CN.md`。该新版纠正了本文件早先对
> weight-decay 后十轮的错误上界判断，并区分主线必需项与条件性扩展。

本地没有公司 ImageNet 最好模型的 checkpoint。9 月 19 日分享包只有 logs / summary / args / 效率测量，**权重按设计未打包**。论文数字已从 `summary.csv` 更新；下面这些必须在你那边的机器上做。

路径前缀按包内记录是 `/root/runs/mergenet/`。

---

## P0（请优先）

### 1. 恢复 `s7_mn_deitinit_150e`（不要重开）

- 状态：105/150，last 在 epoch 104，`last.pth.tar` 完整。
- 这是当前信息量最大的未完成实验：从 DeiT-S/8 300e `model_best.pth.tar` 初始化，加载已确认 `loaded=152, missing=25, unexpected=0`。
- 中途精度在掉（ep20 相对 scratch +64 pp → ep104 只剩 +2.2 pp）。**不要把 79.206 写成终点，也不要外推。**
- 请用原来的 `INITIAL_CHECKPOINT` 路径，`RESUME=auto` 从 `last.pth.tar` 续到 150。
- 续跑后请发：完整 `summary.csv`、launcher banner 里 `resume`/`initial_checkpoint` 行、以及 `verify_init_consumed.py` 或等价的 loaded=152 记录（证明不是 scratch 重跑）。

### 2. 启动 `s7_mn_proglatent_150e`（排队了但没跑）

- 配置：`total_merge_latent: 192`（latent 每层 r=32，落到约 200 token），`drop_path 0.07`，150e，global batch 1024。
- 这是和 ToMe 最接近的结构差：现在所有已完成 MN 都是一次 784→392 悬崖。
- 请单独成表，不要并进 scratch 主表。同样需要 preflight 和 `summary.csv`。

### 3. 不要做的事

- **`s5_mn_wd03_150e` 暂列低优先级。** 已完成 140/150，当前最好 79.054，尚未超过活动晋级门槛 79.716；剩余十个 epoch 没有已知精度上界，不能说“算术上不可能”。若论文要报告完整 weight-decay 消融，应续完；否则仅报告 partial。
- **不要用现在的 DeiT-B（`s6_deitB_p16_300e`）去训 MergeNet-B。** DeiT-B 在 drop_path 0.1 下从 80.442 掉到 78.566。MN 的双ramp 会让它“看起来更正则”，结果不可解释。
- 暂不扩大 LR / warmup / wd / λ 扫描：已完成的四个探针没有越过活动晋级门槛。这不是统计等价证明，wd 仍未完成，也没有穷尽优化方案。
- **不要 600e。** 150→300 时 MN 增益还小于 dense（+2.05 vs +2.25）。

---

## P1（有空再做）

### 4. 如果要做 Base 尺度

先把 DeiT-B 按论文设定改成 **drop_path 0.2** 重跑 300e 锚点，再给 MergeNet-B 配 **有效** 随机深度（不是名义上同写 0.1）。这一对大约要再加一轮 300e。

### 5. 抽出缺失的 EMA 文件

- `s3_mn_r3_ft384_30e` 没有 `ema_weights.pth`，所以 512 只能从 R5.143 那条接。
- 若要用 R=3 在 512 上配对，请用 `extract_ema_weights.py` 从 `s3_mn_r3_ft384_30e/model_best.pth.tar` 抽 EMA，再开一条 15e。非必须。

### 6. 论文可视化还缺的 checkpoint 侧材料（本地做不了）

如果方便传**不违反公司策略**的非权重产物，下面这些最有用：

- 续完后的 `s7_mn_deitinit_150e/summary.csv`
- `s7_mn_proglatent_150e` 的 summary + args
- 若干 ImageNet val 图上的 donor/receiver / 合并可视化（需要权重，所以只能你那边跑）
- 若政策允许：把 `s3_mn_r3_lr75_300e` 和 `s3_deit_p8_300e` 的 EMA 放到可复现评测的机器上，做同 harness、同 `prop_attn` 的精度–延迟联合点。本地 A100 没有这些权重，无法画 verified Pareto。

---

## 本地已经完成、不用再跑的

- ImageNet 224 150e/300e、384 30e、512 15e 精度表
- ToMe/PiToMe 20 点预算扫
- H20 效率与 384 matched latency（prop_attn 仍与精度扫不一致，论文里没有把它们画成 Pareto）
- CIFAR 24 个 200e 三 seed × 两 selector × 四几何（含 degree-matched），checkpoint 在本地 campaign 目录
- 论文稿、表、图已按上述完整结果更新

---

## 最好模型在哪（你那边）

| 用途 | run | 文件 | 备注 |
| --- | --- | --- | --- |
| 主 MN 224 | `s3_mn_r3_lr75_300e` | `model_best.pth.tar`, `ema_weights.pth` | 81.368 @ep291 |
| 主 dense 224 | `s3_deit_p8_300e` | 同上 | 82.246 @ep277 |
| MN 384 | `s3_mn_r3_ft384_30e` | `model_best.pth.tar` **无 ema_weights.pth** | 82.582 @ep20 |
| MN 384 半径匹配 | `s3_mn_r5_ft384_30e` | 有 ema | 82.574；512 从这里初始化 |
| MN 512 | `s4_mn_ft512_15e` | `model_best.pth.tar`, `last.pth.tar` | 82.618 @ep6 |
| DeiT-init（未完成） | `s7_mn_deitinit_150e` | `last.pth.tar` (ep104) | 请 resume |

论文 GitHub：`yukai2007/mergenet-paper`。本地工程：`/liziqing/yukai/mergenet-paper`。
