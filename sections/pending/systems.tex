\subsection{Trained-checkpoint joint evaluation: reserved frontier}
\label{sec:pending-frontier}
\pendingslot{P03}{One numeric packet for the main table and two component interventions}{The
weights remain on the collaborator's machine; only aggregate metrics,
configuration receipts and raw timing samples are returned.}

Evaluate dense DeiT-S/8, matched DTEM, ToMe and \mn{} at 224 pixels and 392
final patch tokens. Bind every row to a checkpoint hash and state key. Use the
same preprocessing, precision, batch, device, attention policy and source
revision; evaluate all 50,000 validation images. Remove token-count hooks
before timing, use synchronized CUDA events on an otherwise idle device, and
archive every latency sample. Report median and IQR, peak allocated and peak
reserved memory, and whether data loading is excluded. ToMe must use
proportional attention in both its accuracy and timing paths.

\begin{table}[htbp]\centering\small
\begin{tabular}{lrrrrr}\toprule
Method & Patches & Top-1 & Median ms & IQR ms & Allocated GiB\\\midrule
Dense DeiT-S/8 & 784 & 82.246 & \pendingvalue & \pendingvalue & \pendingvalue\\
DTEM, matched & 392 & \pendingvalue & \pendingvalue & \pendingvalue & \pendingvalue\\
DeiT-S/8 + ToMe & 392 & 81.932 & \pendingvalue & \pendingvalue & \pendingvalue\\
\mn{} $R=3$ & 392 & 81.360 & \pendingvalue & \pendingvalue & \pendingvalue\\
\bottomrule\end{tabular}
\caption{Reserved common-protocol checkpoint cells [P03]. The three filled
accuracy values are existing records; they are rerun in the common harness
rather than silently paired with random-weight timing.}
\label{tab:pending-frontier}\end{table}

The same packet evaluates two inference-only interventions on the final
\mn{} checkpoint: set the latent $\log m$ key bias to zero, and bypass the
residual recovery attention. Their top-1/top-5 changes test whether the trained
model uses these components; they are not claims about separately trained
architectures.

\begin{table}[htbp]\centering\small
\begin{tabular}{lrr}\toprule
Checkpoint intervention & Top-1 & Top-5\\\midrule
Native \mn{} & 81.360 & \pendingvalue\\
Zero latent mass bias & \pendingvalue & \pendingvalue\\
Disable recovery cross-attention & \pendingvalue & \pendingvalue\\
\bottomrule\end{tabular}
\caption{Reserved inference interventions included in P03.}
\label{tab:pending-components}\end{table}
