\section{Implementation Details}
\label{app:recipe}

Table~\ref{tab:recipe} gives the training settings for the main ImageNet
experiments. All runs use an effective global batch of 1,024. Dense DeiT
uses a learning rate of $5\times10^{-4}$; \mn{}
uses $7.5\times10^{-4}$ for the scratch-training results reported here.
EMA top-1 is measured on the 50,000-image validation set.

\begin{table}[h]
\caption{Training settings for the ImageNet-1K experiments.}
\label{tab:recipe}
\centering\small
\begin{tabular}{lll}
\toprule
Setting & 224-pixel scratch & 384/512-pixel fine-tune\\
\midrule
Epochs & 150 or 300 & 30 at 384; 15 at 512\\
Effective global batch & 1024 & 1024\\
Optimizer & AdamW & AdamW\\
Learning rate & $5\!\times\!10^{-4}$ or $7.5\!\times\!10^{-4}$ & $10^{-5}$\\
Minimum learning rate & $10^{-5}$ & $10^{-6}$\\
Weight decay & 0.05 & $10^{-8}$\\
Warmup epochs & 5 & 1\\
Warmup learning rate & $10^{-6}$ & $10^{-6}$\\
EMA decay & 0.99996 & 0.9996\\
Mixup / CutMix & 0.8 / 1.0 & 0 / 0\\
Label smoothing & 0.1 & 0.1\\
Random erasing & 0.25 & 0.25\\
RandAugment & \texttt{rand-m9-mstd0.5-inc1} & same\\
Gradient clip norm & 1.0 & 1.0\\
Validation crop percentage & 0.9 & 1.0\\
\mn{} auxiliary-logit weight & ramp to 0.05 & 0.05\\
Teacher / feature / routing distillation & disabled & disabled\\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Classifier.}
The patch-8 model has width 384, six heads, six local blocks, and six latent
blocks. At 224 pixels, the local encoder processes 784 patch slots. Local
attention spans 16 sequence positions on either side of a patch query;
the CLS query attends globally. Spatial routing uses radius 3, a
$384\to64$ metric projection, similarity temperature 1, and metric
input-gradient scale 0.1. At compression factor 2, the six soft donor
budgets are $(65,65,65,65,65,67)$; the hard gather retains 392 patches.
The latent encoder does not merge further. Stochastic depth is 0.1,
with separate local and latent ramps.

\paragraph{Routing and selection.}
Training partitions donors and receivers randomly for each image;
evaluation alternates deterministic odd/even partitions across routing
steps. The final gather indices are detached, and selected carriers
are ordered by a detached, mass-weighted center of their original
flattened indices. Spatial scoring uses sparse neighbor lists.

\paragraph{Smooth top-$k$.}\label{app:soft-topk}
For donor energies $e_i$ and step budget $k_\ell$, let
$v_i=30e_i/(\max e-\min e)$. A threshold $\theta$ is determined from
the sorted scores using prefix and suffix exponential sums. The
continuous donor weight is
\begin{equation}
 g_i=\begin{cases}
  1-\tfrac12\exp(\theta-v_i), & v_i>\theta,\\
  \tfrac12\exp(v_i-\theta), & v_i\le\theta.
 \end{cases}
 \label{eq:soft-topk}
\end{equation}
The threshold makes the weights sum to $k_\ell$ up to numerical
precision. Donors without eligible receivers are assigned zero
transport weight. Applying the same relaxation to the final patch
masses supplies the straight-through carrier weights; the hard
top-$K$ indices still determine which slots are gathered.

At training time, an auxiliary full-grid readout contributes up to
0.05 to the latent CLS logits. Its weight begins ramping at epoch 10
for the 150-epoch runs and epoch 20 for the 300-epoch run, reaching
0.05 over 10 and 20 epochs, respectively. It is disabled at evaluation.

\section{Accuracy Budgets}
\label{app:latency}

Table~\ref{tab:sweepdetail} gives the values behind
Figure~\ref{fig:sweep}. The \mn{} checkpoint uses radius 3 at
224 pixels and radius 5.143 after 384-pixel fine-tuning. Each row
matches final patch counts, excluding CLS, but not the layerwise
sequence lengths: ToMe and PiToMe merge across blocks, whereas
\mn{} gathers once between stages. All points evaluate fixed
checkpoints at different inference budgets.

\begin{table}[h]
\caption{ImageNet top-1 (\%) at matched final patch counts.
$\Delta$ is ToMe minus \mn{} in percentage points.}
\label{tab:sweepdetail}
\centering\small
\input{tables/sweep_main}
\end{table}


\section{Training Duration}
\label{app:training-budget}

Table~\ref{tab:training-budget} compares the completed scratch-training
schedules at 224 pixels. Each cell is the best recorded EMA top-1 on
ImageNet-1K validation; it is a single-run training result.
Dense DeiT uses learning rate $5\times10^{-4}$ and \mn{} uses
$7.5\times10^{-4}$. The remaining settings follow Table~\ref{tab:recipe}.

\begin{table}[htbp]
\caption{Best EMA ImageNet-1K top-1 (\%) under two scratch-training schedules.
$\Delta$ is the 300-epoch minus 150-epoch result in percentage points.}
\label{tab:training-budget}
\centering\small
\input{tables/training_budget}
\end{table}

\section{Resolution Transfer}
\label{app:transfer}

Table~\ref{tab:transfer} evaluates the 224-pixel checkpoints at 288 and
384 pixels without fine-tuning. The \mn{} radius remains 3 and the
validation crop percentage remains 0.9. Position embeddings are adapted
to the evaluation grid. These rows are separate from the fine-tuned
384-pixel results, which use validation crop percentage 1.0.
Final patch counts match across the compressed methods in each row.

\begin{table}[htbp]
\caption{ImageNet-1K top-1 (\%) after changing input resolution without
fine-tuning the 224-pixel checkpoints. Patch counts exclude CLS.}
\label{tab:transfer}
\centering\small
\input{tables/resolution_transfer}
\end{table}
